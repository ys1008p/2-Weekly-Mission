import styled from "styled-components";

const StyledModalContainer = styled.div`
  padding: 32px 40px;
  display: flex;
  position: absolute;
`;

function Modal({ children }) {
  <div>{children}</div>;
}
